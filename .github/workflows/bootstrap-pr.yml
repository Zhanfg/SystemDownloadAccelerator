name: Build Alpha 7 In-App Confirmation

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: android-actions/setup-android@v3

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Build and verify Alpha 7
        id: build
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          bash ci/build-alpha7.sh 2>&1 | tee build-alpha7.log

      - name: Upload build diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: SystemDownloadAccelerator-alpha7-build-log
          path: build-alpha7.log
          if-no-files-found: warn

      - name: Stop after build failure
        if: steps.build.outcome != 'success'
        run: exit 1

      - uses: actions/upload-artifact@v4
        with:
          name: SystemDownloadAccelerator-0.2.5-alpha7
          path: dist/*
          if-no-files-found: error
