name: Build Real API 102 Module

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          fetch-depth: 0

      - name: Expand bootstrap project
        shell: bash
        run: |
          set -euo pipefail
          if [[ -f .bootstrap/project.b64 ]]; then
            base64 --decode .bootstrap/project.b64 > /tmp/project.tar.gz
            gzip -t /tmp/project.tar.gz
            tar -xzf /tmp/project.tar.gz
          fi

      - name: Apply system-bar insets
        shell: bash
        run: |
          set -euo pipefail
          SRC="app/src/main/java/io/github/zhanfg/sda"
          mkdir -p "$SRC"
          cat > "$SRC/SystemBarInsets.java" <<'JAVA'
          package io.github.zhanfg.sda;

          import android.graphics.Insets;
          import android.os.Build;
          import android.view.View;
          import android.view.WindowInsets;

          final class SystemBarInsets {
              private SystemBarInsets() {}

              static void apply(View root) {
                  if (Build.VERSION.SDK_INT < Build.VERSION_CODES.KITKAT_WATCH) {
                      root.setFitsSystemWindows(true);
                      return;
                  }
                  final int baseLeft = root.getPaddingLeft();
                  final int baseTop = root.getPaddingTop();
                  final int baseRight = root.getPaddingRight();
                  final int baseBottom = root.getPaddingBottom();
                  root.setOnApplyWindowInsetsListener((view, insets) -> {
                      int left;
                      int top;
                      int right;
                      int bottom;
                      if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.R) {
                          Insets bars = insets.getInsets(
                                  WindowInsets.Type.systemBars() | WindowInsets.Type.displayCutout());
                          left = bars.left;
                          top = bars.top;
                          right = bars.right;
                          bottom = bars.bottom;
                      } else {
                          left = insets.getSystemWindowInsetLeft();
                          top = insets.getSystemWindowInsetTop();
                          right = insets.getSystemWindowInsetRight();
                          bottom = insets.getSystemWindowInsetBottom();
                      }
                      view.setPadding(baseLeft + left, baseTop + top,
                              baseRight + right, baseBottom + bottom);
                      return insets;
                  });
                  root.requestApplyInsets();
              }
          }
          JAVA

          python3 - <<'PY'
          from pathlib import Path
          import re

          root = Path('app/src/main/java/io/github/zhanfg/sda')
          main = root / 'MainActivity.java'
          text = main.read_text(encoding='utf-8')
          if 'SystemBarInsets.apply(rootView);' not in text:
              text, count = re.subn(
                  r'(?:this\.)?setContentView\((?:this\.)?buildRoot\(\)\);',
                  '''android.view.View rootView = buildRoot();
                  SystemBarInsets.apply(rootView);
                  setContentView(rootView);''',
                  text,
                  count=1,
              )
              if count != 1:
                  raise SystemExit('MainActivity inset patch failed')
              main.write_text(text, encoding='utf-8')

          old = root / 'DownloadConfirmActivity.java'
          text = old.read_text(encoding='utf-8')
          if 'SystemBarInsets.apply(rootView);' not in text:
              pattern = r'(?:this\.)?setContentView\((?:this\.)?build\(([^;]+?)\)\);'
              match = re.search(pattern, text)
              if not match:
                  raise SystemExit('DownloadConfirmActivity inset patch failed')
              arg = match.group(1)
              text = re.sub(pattern, f'''android.view.View rootView = build({arg});
                  SystemBarInsets.apply(rootView);
                  setContentView(rootView);''', text, count=1)
              old.write_text(text, encoding='utf-8')
          PY

      - name: Wire browser confirmation hook
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          module = Path('app/src/main/java/io/github/zhanfg/sda/xposed/RealDownloadAcceleratorModule.java')
          text = module.read_text(encoding='utf-8')
          old = '''    @Override
              public void onPackageReady(XposedModuleInterface.PackageReadyParam param) {
                  if (!TARGET_PACKAGE.equals(param.getPackageName())) {
                      return;
                  }

                  final ClassLoader classLoader = param.getClassLoader();
                  for (String className : TRANSFER_CLASSES) {
                      try {
                          installTransferHooks(classLoader, className);
                      } catch (Throwable error) {
                          log(Log.ERROR, TAG, "Failed to install real engine hooks for " + className, error);
                      }
                  }
              }
          '''
          new = '''    @Override
              public void onPackageReady(XposedModuleInterface.PackageReadyParam param) {
                  if (TARGET_PACKAGE.equals(param.getPackageName())) {
                      final ClassLoader classLoader = param.getClassLoader();
                      for (String className : TRANSFER_CLASSES) {
                          try {
                              installTransferHooks(classLoader, className);
                          } catch (Throwable error) {
                              log(Log.ERROR, TAG, "Failed to install real engine hooks for " + className, error);
                          }
                      }
                      return;
                  }

                  try {
                      DownloadConfirmationHook.install(this, param);
                  } catch (Throwable error) {
                      log(Log.ERROR, TAG,
                              "Failed to install download confirmation hook in "
                                      + param.getPackageName(), error);
                  }
              }
          '''
          if 'DownloadConfirmationHook.install(this, param)' not in text:
              if old not in text:
                  raise SystemExit('Real module onPackageReady block not found')
              text = text.replace(old, new, 1)
              module.write_text(text, encoding='utf-8')

          manifest = Path('app/src/main/AndroidManifest.xml')
          xml = manifest.read_text(encoding='utf-8')
          activity = '''
                  <activity
                      android:name=".InterceptDownloadConfirmActivity"
                      android:exported="true"
                      android:excludeFromRecents="true"
                      android:noHistory="true" />
          '''
          if '.InterceptDownloadConfirmActivity' not in xml:
              xml = xml.replace('</application>', activity + '\n    </application>', 1)
              manifest.write_text(xml, encoding='utf-8')
          PY

      - name: Configure API 102 module metadata
        shell: bash
        run: |
          set -euo pipefail
          test -f app/src/main/java/io/github/zhanfg/sda/xposed/RealDownloadAcceleratorModule.java
          test -f app/src/main/java/io/github/zhanfg/sda/xposed/DownloadConfirmationHook.java
          test -f app/src/main/java/io/github/zhanfg/sda/InterceptDownloadConfirmActivity.java

          META="app/src/main/resources/META-INF/xposed"
          mkdir -p "$META"
          printf '%s\n' 'io.github.zhanfg.sda.xposed.RealDownloadAcceleratorModule' > "$META/java_init.list"
          cat > "$META/scope.list" <<'EOF'
          com.android.providers.downloads
          mark.via.gp
          mark.via
          com.android.chrome
          com.heytap.browser
          com.microsoft.emmx
          org.mozilla.firefox
          com.sec.android.app.sbrowser
          com.android.browser
          EOF
          cat > "$META/module.prop" <<'EOF'
          minApiVersion=102
          targetApiVersion=102
          staticScope=false
          autoHotReload=false
          EOF

          cat >> app/proguard-rules.pro <<'EOF'

          -keep class io.github.zhanfg.sda.xposed.RealDownloadAcceleratorModule { public *; }
          -keep class io.github.zhanfg.sda.xposed.RealDownloadAcceleratorModule$* { *; }
          -keep class io.github.zhanfg.sda.xposed.DownloadConfirmationHook { *; }
          -keep class io.github.zhanfg.sda.xposed.DownloadConfirmationHook$* { *; }
          -keep class io.github.zhanfg.sda.InterceptDownloadConfirmActivity { public *; }
          EOF

      - name: Normalize Android build toolchain
        shell: bash
        run: |
          cat > build.gradle.kts <<'EOF'
          plugins {
              id("com.android.application") version "8.7.3" apply false
          }
          EOF
          cat > settings.gradle.kts <<'EOF'
          pluginManagement {
              repositories {
                  google()
                  mavenCentral()
                  gradlePluginPortal()
              }
          }
          dependencyResolutionManagement {
              repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
              repositories {
                  google()
                  mavenCentral()
              }
          }
          rootProject.name = "SystemDownloadAccelerator"
          include(":app")
          EOF
          cat > gradle.properties <<'EOF'
          org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
          android.useAndroidX=false
          android.nonTransitiveRClass=true
          EOF
          cat > app/build.gradle.kts <<'EOF'
          plugins {
              id("com.android.application")
          }

          android {
              namespace = "io.github.zhanfg.sda"
              compileSdk = 35
              buildToolsVersion = "35.0.0"

              defaultConfig {
                  applicationId = "io.github.zhanfg.sda"
                  minSdk = 26
                  targetSdk = 35
                  versionCode = 4
                  versionName = "0.2.1-alpha3"
              }

              buildTypes {
                  debug {
                      applicationIdSuffix = ".debug"
                      versionNameSuffix = "-debug"
                  }
                  release {
                      isMinifyEnabled = true
                      isShrinkResources = true
                      proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
                      signingConfig = signingConfigs.getByName("debug")
                  }
              }

              compileOptions {
                  sourceCompatibility = JavaVersion.VERSION_17
                  targetCompatibility = JavaVersion.VERSION_17
              }

              packaging {
                  resources {
                      merges += "META-INF/xposed/*"
                  }
              }

              lint {
                  abortOnError = false
                  checkReleaseBuilds = false
              }
          }

          dependencies {
              compileOnly("io.github.libxposed:api:102.0.0")
          }
          EOF

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: android-actions/setup-android@v3

      - name: Install Android SDK
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Build APKs
        run: gradle --no-daemon :app:assembleDebug :app:assembleRelease

      - name: Verify confirmation packaging
        shell: bash
        run: |
          set -euo pipefail
          for apk in app/build/outputs/apk/debug/app-debug.apk app/build/outputs/apk/release/app-release.apk; do
            test "$(unzip -p "$apk" META-INF/xposed/java_init.list | tr -d '\r\n')" = \
              "io.github.zhanfg.sda.xposed.RealDownloadAcceleratorModule"
            unzip -p "$apk" META-INF/xposed/scope.list | grep -qx 'mark.via.gp'
            unzip -p "$apk" META-INF/xposed/module.prop | grep -qx 'targetApiVersion=102'
            unzip -p "$apk" classes.dex | strings | grep -q 'InterceptDownloadConfirmActivity'
            unzip -p "$apk" classes.dex | strings | grep -q 'Download confirmation hook installed in'
          done

      - name: Prepare artifacts
        run: |
          mkdir -p dist
          cp app/build/outputs/apk/debug/app-debug.apk dist/SystemDownloadAccelerator-0.2.1-alpha3-debug.apk
          cp app/build/outputs/apk/release/app-release.apk dist/SystemDownloadAccelerator-0.2.1-alpha3.apk
          cd dist
          sha256sum *.apk > SHA256SUMS.txt

      - uses: actions/upload-artifact@v4
        with:
          name: SystemDownloadAccelerator-0.2.1-alpha3
          path: dist/*
          if-no-files-found: error
